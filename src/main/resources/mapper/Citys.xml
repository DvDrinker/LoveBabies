<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lanou.dao.CitysMapper">
	<resultMap id="CitysResult" type="Citys">
	</resultMap>
	<select id="selectCitys" resultMap="CitysResult">
		select * from CITYS where parentId = #{parentId}
	</select>
</mapper>
